
Промени от Надетто 14/15.юни.2014 :D
За преглед и одобрение от тима

1. Файлова система
2. Функционалност
3. Качествен код
4. Дизайн
5. Други

1. Файлова система
	- "lib" folder
		като гледах другите проект и от видеата, Js файловете се слагат в отделна папка. И там се трупат различните видове
	- uess-the-rect.js -  отделен .js файл, в който в събран целия JavaScript code. Всички лектори и навсякъде пише, че в HTML файла не трябва да има JavaScript
	-js - файловете се слагали, гледах по лекциите, възможно най надолу в тялото на HTML за да може да не се стартират преди цялото съдържание на HTML да се е заредило
	
	
2. Функционалност
	- добавих var count, която ти позволява на един грешен опит за всяко ниво, преди да загубиш играта, ако имаш грешка count се намаля, ако уцелиш count връща първоначалната си стойност
	- разделих този if  на логичните му варианти и добавих коментари. Така може много по-лесно да се добавят функционалности, без кода да е обвързан с повече от едно условие
			if ((selectedIndex === random) && (level < 5)) {
	- if (selectedIndex === random) {
                if (level < 4) {
		Тук може да се сменя числото надолу - в момента позволява 7 куба общо. И понеже има функция за 1 позволена грешка, играта не свършва мн бързо

 
3.   - изтрих едната инициализация на var misty. В JavaScript има скоуп само във функциите. if, else, for и др не образуват скоуп и следователно не е нужно misty да се пише
		2 пъти, защото така има повтаряне на код: DRY principle - "Don't repeat yourself" 
		изтривам var level = 0;         - реда се повтаря на 20 и 22-ри ред
		             stage.add(layer);   - по надолу има такъв ред - един е достатъчен
		
	- Малко промених положението на кейсовете в if/else. Следвайки, или опитвайки се да следвам:D, това, което ни учат в "Качествен програмен код": Първо най-логичните, или първо най-очакваните, после изброятат се 
		всички кейсове от един тип, логически разделени...
	- добавих празни редове - за да може по-лесно визуално да се проследи логиката на играта: КПК: след всеки знак: } има задължително празен ред. След края на функция има празен ред.
		
		
4. Извинявайте, че ви промених дизайна - опитвах се възможно най-малко:)
	- цвят на кубчетата, и заоблени ръбове - за да са по-приблекателни
	- strike на кубчетата - за да се открояват, въздействат по-добре
	- strike и по-дебел border на рамката - 
	- вкарах заглавието на играта в самата граница и уеднаквих дизайна с целия тон на играта - търсейки хармония на елементите и пасване с другите елементи,
		за да може да се предизвика чувство на цялостност
		
5. Други: ?:D
	- слагам това на var random, за да мога да тествам приложението и за да мога да си осигуря, че всеки път ще уцелвам кубчето
	var random = selectedIndex //Math.floor(Math.random() * (level + 3));