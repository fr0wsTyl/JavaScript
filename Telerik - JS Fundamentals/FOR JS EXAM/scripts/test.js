function Solve(input) {
    var parse = input.map(Number);
    var result = 1;
    for (var i = 0; i < parse.length; i++) {
    	parse[i]
    };
    return result;
}
var array = ['1', '3', '-5', '8', '7', '-6'];
console.log(Solve(array));
